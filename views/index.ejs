<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>API</title>
    <link rel="stylesheet" href="styles/main.css">
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" integrity="sha512-SnH5WK+bZxgPHs44uWIX+LLJAJ9/2PkPKZ5QiAj6Ta86w+fsb2TkcmfRyVX3pBnMFcV7oQPJkl9QevSCWr3W6A==" crossorigin="anonymous" referrerpolicy="no-referrer" />
</head>
<body>
    
    <div class=" container col-12 col-md-8 col-lg-6">
    <form action="/submit" method="POST" id="f">
        <i class="fa-solid fa-location-dot" id="map"></i>
        <input type="text" name="city" id="box" placeholder="enter location">
      <button type="submit" id="search">search</button>
      <h5 id="erro">please enter a location</h5>

        
            
     
      <img src="" id="cloud">
        <h4 class="p" id="location"><%= w ? w.name : e  ||o%></h4>
    <p class="h3" id="degree"><%= w ? w.main.temp : e ||o %> °C</p>
        <i class="fa-solid fa-wind" id="wind"></i>
        <p class="h4" id="speed"><%= w ? w.wind.speed: e  ||o%> Km/hr</p>
      <h6 id="head">Wind Speed</h6>
     <i class="bi bi-droplet-fill" id="hum"></i>
        <p class="h4" id="humidity"><%= w ? w.main.humidity: e || o%> %</p>
        <h6 id="head2">Humitidy</h6>

       
</div>

    <script>
     const inputBox = document.getElementById('box');
  const mapIcon = document.getElementById('map');
      const form = document.getElementById("f");
        const cc = document.getElementById("cloud");
      const ak = document.getElementById("search");
    const msg= document.getElementById("erro");

        inputBox.addEventListener('input', () => {
            if (inputBox.value) {
         mapIcon.style.left = '30px';
                
            } else {
      mapIcon.style.left = '65px';
            }
        });

        ak.addEventListener("click", () => {if(inputBox.value==""){
                msg.style.visibility="visible";
            }
            else{

            event.preventDefault();
            form.style.height ="500px";
            cc.style.visibility="visible";
            document.getElementById("f").submit(); 
        }});



       </script> 
 
</body>
</html>